machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.3.1

dependencies:
  pre:
    - mv config/secrets.ci.yml config/secrets.yml

deployment:
  deploy:
    branch: master
    commands:
      - bundle exec cap production deploy:
          timeout: 1200
